# CLAUDE.md

## 이 앱이 무엇인가
HR 근속(재직 기간) 분석 대시보드 — hr_01_dashboard 도메인 데이터 기반

## 그레인
사번(직원) 1명 = 1행

## 퍼널 단계와 각 단계의 조건 (획득 퍼널, funnel())

입사 -> 1년차_잔류 -> 3년차_잔류 -> 5년차_잔류 -> 10년차_잔류

조건: HR_직원 + HR_퇴사이력을 조인해 재직 여부/퇴사일을 구하고,
lifelines.KaplanMeierFitter로 생존함수 S(t)를 추정한다.
재직 중인 사람의 종료 시점은 현재 시각이 아니라 config.SNAPSHOT_DATE(고정값)를 쓴다.
각 연차의 n = round(S(t) * 300).

## 유지 퍼널과 지표(kpis/monthly)
Day2에 구현 완료. "미정" 아님.

유지 퍼널(retention_funnel()) 조건: "초과근무 고부담 지속" — 상위진입
(해당 월 초과근무시간이 config.OVERTIME_TOP33_HOURS, 즉 내도메인.md
명세4행 삼분위 경계 16.6시간/월 이상) -> 2개월연속 -> 3개월연속. 그레인은
사번 1건(사번당 최장 연속 상위진입 개월 수 하나).

kpis() 4개 지표: 재직인원 · 입사1년내이탈률(초과근무 상위 1/3 세그먼트
기준, 전사 기준값은 참고용으로 별도 필드) · 평균평가점수(재직자만) ·
월평균초과근무시간.

monthly()는 이 중 재직인원 · 월평균초과근무시간 두 개만 월별 시계열로
낸다. 입사1년내이탈률은 매달 KM을 다시 적합해야 해서 뺐고, 평균평가점수는
HR_평가가 분기 단위라 월로 못 나눈다.

## 임계값
Day1에 확정. "미정" 아님.

config.THRESHOLDS: 입사1년내이탈률 경고 6%/위험 8%(초과근무 상위 1/3
세그먼트 기준으로 계산해야 이 값과 맞는다), 평균평가점수 경고 2.7점/
위험 2.7점(문서에 위험선만 있어 동일하게 씀).
config.MIN_SAMPLE: 30(전사 300명 기준으로 낮춘 값).

## Day3 — 판정 카드(trust_check · 주지표/가드레일)
못 믿을 조건: 표본이 config.MIN_SAMPLE(30) 미만이면 못 믿는다 — 조건
1개로 충분하다(실험 없음 + 위험집합이 이미 다 MIN_SAMPLE 통과 확인됨).
배정(SRM)·기간 조건은 넣지 않았다.

판정 순서(고정): 못 믿을 조건 -> 주지표 -> 가드레일. 못 믿을 때는
계산하지 않는다(계산해 놓고 숨기는 것이 아니다). 걸린 항목에는 사유만
남기고 수치는 남기지 않는다.

판정 카드: 주지표 = 재직인원(전월 대비 3% 이상 증가), 가드레일 =
월평균초과근무시간(같은 기간 5% 이상 악화). 관측 기간(15개월) 전체에서
"효과 없음"만 나왔다 — 임계값이 잘못돼서가 아니라 이 기간 동안 재직인원이
한 번도 3%만큼 움직인 적이 없었기 때문이다(근거: 판단기준.md).

## Day4 — 리포트 8장(자동/사람 구분·인과 검사·한계 절)
대시보드(pages/2_대시보드.py)의 분해 축·구간 필터는 st.query_params와
연동한다. 축은 st.segmented_control로 고르고, 페이지가 뜰 때 query_params
에서 초기값을 읽는다. 없는 값·이상한 값이 오면 에러를 내지 않고 조용히
기본값으로 떨어진다. 필터 위젯은 @st.fragment 밖에 둔다 — fragment 안에서
query_params를 갱신하면 URL과 화면이 어긋날 수 있기 때문이다.

사람이 쓰는 장/자동 조립 장 구분: 2·6·8장(배경·해석·제안)은 pages/3_리포트.py의
st.form 하나로 묶어 사람이 쓴다. 나머지(1·3·4·5·7장)는 report/sections.py가
metrics.py·validate.py 결과를 그대로 옮겨 자동 조립한다. 틀렸을 때 사람이
책임져야 하는 장(해석·제안 등)은 자동화하지 않는다.

report/sections.py의 BANNED 목록: 기본 6개(때문에·덕분에·효과로·입증되었·
증명되었·확실히)에 도메인 표현 2개(기여도가 높다·영향을 미쳤다)를 추가했다.
check_phrasing()은 자동 생성 장뿐 아니라 사람이 쓰는 장(폼 제출 시)에도 반드시
건다 — 걸려도 저장은 하고 화면 경고만 남긴다. assert로 앱을 죽이지 않는다.

"7. 한계" 절(_s7_limits())은 사람이 매번 쓰지 않고 자동 조립한다:
validate.run_checks()의 warn 전체 + funnel_by() 각 칸의 trust_check() 결과
(표본 부족으로 판정 못 한 항목 — 사유만 적고 수치는 안 적음) + 항상 넣는 두
고정 문장(인과 주장 불가 · 관측 기간 제약, config.SNAPSHOT_DATE·데이터
기간에서 읽음, 하드코딩 아님) + "확인하지 못한 것" 3줄(사람이 직접 채움).
pages/3_리포트.py에서 st.data_editor로 편집 가능하고, "포함" 체크를 끄면
리포트·PDF에서 빠진다.

리포트 화면·PDF의 분해 차트(device_png)도 대시보드와 같은 trust_check()
마스킹을 반드시 적용한다 — 표본 부족 칸은 수치 없이 사유만 넣는다.

## 제안 리포트(report/proposal.py)
report/proposal.py는 report/sections.py와 같은 자동/사람 분리 원칙을
따른다.

자동 절에 카드(제안카드.md)에 없는 값을 채워 넣지 않는다. 없으면 "todo"로
남긴다.

문장 검사(check_phrasing)를 새로 만들지 않고 sections.py 것을 재사용한다.

## 제안서 구조 확정(core/metrics.py proposal_topics·topic_evidence 기반)
기존 제안카드.md 기반 report/proposal.py와 별개로, proposal_topics()·
topic_evidence()가 실제로 채울 수 있는 절만으로 처음부터 다시 짠 구조다.
템플릿(제안서_템플릿.html)은 참고하지 않았다.

절 순서는 교안 판단기준 ①(현황→원인→규모→제안, "순서를 바꾸면 설득이
안 된다")을 따른다. "무엇을 제안하는가"는 본문 절이 아니라 결론을
근거보다 먼저 보여주는 오리엔테이션 문장이라 본문에서 빼고 **문서 맨
위 한 장 요약 박스**로 옮겼다 — monthly_verdict()의 "판정 순서가
전부다" 원칙과 같은 이유로, 결론을 본문 첫 절에 두면 설득 구조가
깨진다. 추세는 교안 4단계엔 없지만 "지금 결정해야 하는가, 더
지켜봐도 되는가"라는 별개 질문에 답하므로 규모 뒤에 별도 절로 남겼다
(제안이 본문에서 빠졌으니 "규모 뒤·제안 앞" 제약은 자동으로 충족된다).

**문서 맨 위 — 한 장 요약**(본문 절 수에 안 들어감): `proposal_topics(t)`
상위 1건(기각 제외)의 제목·한줄. 자동.

**본문 6절**:

| # | 절 제목(질문형) | 이 절이 답하는 질문 | 채우는 데이터 함수 | 자동·사람 |
|---|---|---|---|---|
| 1 | 지금 상황이 어떤가? | 이 문제가 실제 데이터에서 확인되는가, 전체 흐름 중 어느 단계에서 발생하는가? | `topic_evidence(t, topic)["현황"]` | 자동 |
| 2 | 원인이 무엇인가? | 조직의 어느 축(부서·직급·채용경로)에서, 어떤 크기로 갈리는가 — 손을 쓸 수 있는 지점이 있는가? | `topic_evidence(t, topic)["원인"]` (구간 비교형 문제에만 존재. 그 외엔 "없음+사유") | 자동 |
| 3 | 방치하면 연간 얼마나 손실인가? | 연간 몇 명/몇 건 규모의 문제이며, 그 환산에 어떤 가정이 들어갔는가? | `topic_evidence(t, topic)["규모"]` | 자동 |
| 4 | 최근 추세는 어떤가? | 최근 나아지고 있는가 나빠지고 있는가 — 지금 결정해야 하는가, 더 지켜봐도 되는가? | `topic_evidence(t, topic)["추세"]` (월별 추적 가능한 지표에만 존재. 그 외엔 "없음+사유") | 자동 |
| 5 | 위험과 철회 기준은 무엇인가? | 이 제안이 틀렸을 때 무엇을 보고 알아채고, 어떻게 되돌리는가 — 조건부 승인이라면 조건은 무엇인가? | 없음 — 사람이 직접 작성 | 사람 |
| 6 | 무엇을 승인해 달라는 것인가? | 팀장에게 정확히 무엇을(예산·인력·기한 등) 해달라는 것인가? | 없음 — 사람이 직접 작성 | 사람 |

**고려했으나 뺀 절(이 도메인에는 없음)** — 채울 함수가 없어 절 자체를
만들지 않았다:
- 비용(예산)은 얼마인가? — 규모 절이 영향 인원(연간 건수)은 내지만 원화 비용·인건비 데이터가 없다.
- 업계·경쟁사 대비 우리 위치는? — 외부 벤치마크 데이터가 없다.
- 과거에 비슷한 시도가 있었는가? — 개입 이력을 추적하는 테이블이 없다.

이 구조는 아직 코드로 옮기지 않았다(표만 확정). 구현은 다음 단계.

## 제안서 규칙 확정 — 문구·그래프·확장 기준

PROPOSAL_SECTIONS는 core/config.py에서만 정의한다 — report/proposal.py·
viz/proposal_charts.py·pages/5_제안서.py 등 다른 파일에 절 제목·질문을
하드코딩하지 않는다(전부 config.PROPOSAL_SECTIONS를 읽어 쓴다).

절을 새로 추가하거나 빼는 기준: **"이 절을 채울 데이터 함수 이름을
지금 댈 수 있는가?"** 댈 수 없으면 절을 만들지 않는다(위 "고려했으나
뺀 절" — 비용·업계 비교·과거 시도 이력 — 이 이 기준으로 빠진 예다).

그래프 규칙: 절 하나에 근거 차트는 최대 1개. 색은 3개 이하만 쓰고
config.COLORS에 이미 있는 값만 재사용한다(새 색을 만들지 않는다) —
viz/proposal_charts.py가 이 규칙을 따른다.

팀장이 화면·인쇄물에서 보는 고정 문구(안내문·배지·버튼 라벨·
placeholder 등 — 데이터로 채워지는 문장은 제외)는 전부
core.config.PROPOSAL_WORDS를 거쳐서만 나온다. report/proposal.py·
viz/proposal_charts.py·pages/5_제안서.py 어디에도 그 문구를 코드에
직접 박아넣지 않는다.

## 코드를 고칠 때 지킬 것
1. 지표 정의는 core/config.py 에서만 바꾼다. metrics.py 안의 계산 로직은 config 값을 참조하되, 임의로 새 지표/임계값을 만들지 않는다.
2. 계산 경로에 현재 시각(pd.Timestamp.now() 등)을 넣지 않는다. 재현성을 위해 항상 config.SNAPSHOT_DATE 같은 고정 기준일을 쓴다.
3. 시키지 않은 파일은 고치지 않는다.
4. 판단이 필요한 지점(임계값, 그레인, 퍼널 조건 등)은 스스로 정하지 말고 나에게 먼저 물어본다.
