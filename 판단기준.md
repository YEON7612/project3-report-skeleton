# 판단 기준

## 2026-09-01 (8주차 Day1)

### 오늘 배운 것

**앱이 데이터를 거부할 수 있는가**
거부하지 못하는 앱은 무엇이든 통과시킨다. 컬럼이 하나 빠져도, 날짜가 어긋나도 계산을 해버리고, 그 위에 지은 모든 화면이 틀린다.

**차단과 경고를 가르는 기준**
규칙을 하나 만들 때마다 묻는 것은 하나뿐이다 — "이 규칙이 깨진 채로 계산하면 값이 틀리는가?" 틀린다 → 차단. 해석만 조심하면 된다 → 경고. 이 판정을 붙이는 사람이 이제 나 자신이라는 것이 달라지는 점이다.

**검증은 적게 만든다**
차단을 많이 걸면 꼼꼼해 보이지만, 실무 데이터는 늘 어딘가 깨져 있어서 앱이 자꾸 멈추고, 결국 사람이 차단을 하나씩 끄기 시작하며, 검증 전체가 무력해진다. 검증은 많을수록 좋은 것이 아니라 지켜지는 만큼만 있는 것이 좋다. 차단은 "이대로 계산하면 확실히 틀리는 것"에만 건다. 나머지는 경고다. 깨져도 계산에 영향이 없는 것은 아예 만들지 않는다.

**임계값에는 근거를 적는다**
"몇 개 미만이면 차단인가"에는 정답이 없다. 근거로 쓸 수 있는 것: 직전 기간, 업계 기준, 분석이 성립하는 최소치, 조직의 합의. 어느 것이든 되지만 한 줄로 남겨야 한다. 근거 없는 임계값은 남의 것이다 — 그대로 쓰면 나중에 "왜 이 값이죠?"에 답할 수 없다. 표본 수가 원래 적은 도메인에서 큰 임계값을 요구하면 어떤 세그먼트도 충족하지 못한다. 낮추되 경고 자체를 없애지는 않는다.

**메시지에는 숫자를 넣는다**
"행 수 부족"이 아니라 "12행 (최소 100)"처럼 실제 값과 기준값을 함께 적는다. 그냥 "실패"로는 아무것도 판단할 수 없다. 숫자가 있으면 얼마나 모자란지가 보이고, 그에 따라 대응이 달라진다. 경고라면 왜 정상일 수도 있는지도 함께 적는다 — 사람이 넘길 근거가 된다.

**구조적 결측을 만나면**
빈 칸이 다 오류는 아니다. 이번 기간에 그 사건이 일어나지 않아서, 그 경로를 거치지 않아서, 아직 그 단계에 도달할 시간이 없어서 비어 있는 것은 정상이다. 필수 입력인데 비어 있는 값만 오류다. 구조적 결측을 채우면 데이터를 위조하는 것이다 — 없는 것이 사실이기 때문이다. 평균이나 0으로 채우면 "그 조건에 해당하지 않는 대상"이 "값이 0인 대상"으로 둔갑한다. 이런 결측은 경고로 띄우되 차단하지 않는다. 다만 조인할 때 조용히 사라지므로 그 대상이 어디로 갔는지 알고 있어야 한다.

**검증은 만든 것으로 끝나지 않는다**
실제로 막히는지 눈으로 봐야 한다. 경고만 띄우고 그냥 진행되는 검증은 없는 것과 같다. 확인할 것 셋 — 차단 표시가 뜬다, 차단 개수가 세어진다, 통과 버튼이 잠긴다. 마지막이 가장 중요하다. 버튼이 안 잠기면 그 검증은 장식이다. 앱이 에러로 죽는 것과 차단으로 멈추는 것은 다르다 — 죽으면 사람이 판단할 화면 자체가 없다.

**판단 근거를 남긴다**
"확인함" 같은 말은 근거가 아니다. 무엇을 보고 왜 괜찮다고 판단했는지 적는다. 경고가 없다면 "경고 없음, 검증 n건 전부 통과"라고 명시적으로 적는다. 아직 다음 단계로 갈 시간이 없는 대상 때문에 비율이 낮게 나올 수 있다 — 이것을 "성과가 나쁘다"로 읽으면 틀린다. 관측 기간 문제와 실제 문제를 구분해야 한다.

**차단/경고의 공통 패턴**
차단 = 키가 없거나 · 중복이거나 · 순서가 역행하거나 · 값이 불가능하다.
경고 = 비어 있거나 · 적거나 · 치우쳐 있다.

**"설정 하나만 고치면 된다"는 절반만 맞다**
이름·기간·단계를 내 것으로 바꾸고 데이터를 연결한 뒤 앱을 켜면 깨진다. 컬럼 이름이 계산 코드 안에도 박혀 있기 때문이다. 한 번에 되면 오히려 뭔가 안 바꾼 것이다.

### 오늘 내가 실제로 내린 결정

- **행 수 규칙을 차단으로 정했다** — 표본이 최소 표본 미만이면 어떤 코호트 분석도 성립하지 않기 때문이다.
- **필수 컬럼 규칙을 차단으로 정했다** — 그 컬럼들이 없으면 잔류율 퍼널과 유지/이탈 판정 자체를 계산할 수 없기 때문이다.
- **날짜 범위 규칙을 차단으로 정했다** — 기간 밖 데이터가 섞이면 집계값이 그대로 왜곡되기 때문이다.
- 오늘 만든 세 규칙은 각각 "이게 깨지면 계산이 틀리는가"를 물었을 때 셋 다 "틀린다" 쪽으로 나와 전부 차단으로 걸었다. 별도의 경고 규칙은 아직 추가하지 않았다.
- **최소 표본을 30으로 잡았다** — 이미 다른 분석에서 쓰고 있던 "코호트 분석이 성립하는 최소 표본" 기준을 그대로 가져왔다. 기본으로 주어진 값은 표본 수 자체가 작은 도메인에는 맞지 않아 낮췄다.

## Day2 — 계산 구현

### 오늘 실제로 내린 판단
- funnel(): 독립 코호트 비율 방식이 아니라 lifelines.KaplanMeierFitter로
  구현. 근거: 내도메인.md의 기준값 자체가 KM 방식으로 계산되어 있었음
  (이탈률 누적 + 잔류율 = 100% 정확히 일치하는 것으로 확인).
- 재현성: funnel()에서 재직 중인 사람의 종료 시점을 pd.Timestamp.now()
  대신 config.SNAPSHOT_DATE(고정값, 2026-03-31)로 사용. 이유: 계산 경로에
  현재 시각이 들어가면 앱을 켤 때마다 결과가 달라져 재현 불가능해짐.
- retention_funnel(): "초과근무 고부담 지속" 개념(상위진입→2개월연속→
  3개월연속)으로 구현. 임계값 16.6시간/월은 내도메인.md 삼분위 경계값을
  그대로 사용, 새로 만들지 않음.
- kpis()의 입사1년내이탈률: 전사 전체(300명, 2.7%)가 아니라 초과근무
  상위 1/3 세그먼트(약 100명, 3.9%)로 모집단을 좁혀서 계산. 이유:
  THRESHOLDS(경고6%/위험8%)가 그 모집단 기준으로 캘리브레이션된 값이라,
  다른 모집단에 적용하면 임계값과 지표가 서로 안 맞음. 전사 전체 값은
  참고용으로 별도 필드에 남김.
- 평가점수 가드레일: 경고선=위험선=2.7로 동일하게 설정. 문서에 위험선
  하나만 있어서, 없는 경고선을 새로 만들지 않고 있는 값만 사용.
- MIN_SAMPLE: 1,000 → 30으로 하향. 이유: 전체 표본이 300명뿐이라 1,000
  기준으로는 모든 지표가 항상 "표본 부족"으로 표시되어 무의미했음.

### Claude가 스스로 정했다가 다시 확인받은 것
- funnel() 초기 구현은 독립 코호트 비율 방식으로 임의로 만들어졌고,
  기준값과 크게 어긋나(10년차 -60.5%p) 재구현이 필요했음.
- 재직 중인 사람의 종료 시점에 pd.Timestamp.now()를 쓴 것 — 명시적으로
  지시하지 않았는데 Claude가 판단해서 넣은 값이었고, 재현성 문제를
  일으켜 SNAPSHOT_DATE로 교체함.

### 부수적으로 고친 버그
- core/validate.py 의 날짜 범위 검증이 core/load.py의 메모리 최적화
  (category dtype 변환)와 충돌해 TypeError 발생. 원인: validate.py가
  이미 있던 안전 변환 헬퍼 to_dt()를 안 쓰고 pd.to_datetime을 직접
  호출했기 때문. to_dt()를 쓰도록 한 줄만 수정. load.py는 건드리지 않음.

### 오늘 비워/미정으로 남긴 것
- 대시보드 화면에 유지 퍼널이 새로 노출됨에 따라, "참고용_전사_입사1년내
  이탈률"을 화면에 같이 보여줄지는 미정.
- device/channel 분해축(funnel_by) — Day3 실습 범위, 오늘 자리만 NotYet
  처리.

## Day3 — 오늘 배운 것 (도메인 무관)

### 오늘 내린 결정

- **무엇으로 쪼갰는가**: 분해 축은 "손을 쓸 수 있는 것"만 후보로 남겼다. 식별자·날짜, 그리고 지금 보려는 결과와 같은 것(그레인·결과 그 자체)은 후보에서 뺐다 — 결과와 같은 변수로 쪼개면 "그 결과인 것만 그 결과다"라는 순환논리가 되기 때문이다.
- **어떤 조건이면 못 믿는다고 정했는가**: 조건은 "표본이 최소 기준 미만이면 못 믿는다" 하나만 뒀다. 비교 대상이 없고(예: A/B 실험 부재) 나머지 조건(예: 관측이 충분히 쌓였는가)은 이미 별도로 확인해 전부 최소 기준을 넘는 것으로 나왔기 때문에, 조건을 더 추가하지 않았다 — 조건은 많을수록 안전해 보이지만 실제로 지켜지는 만큼만 의미가 있다.

### 오늘 실제로 있었던 판단

- **판정 임계값을 실제로 검증했더니 한 번도 안 걸렸다.** 정해둔 두 임계값(하나는 "이 정도 늘면 개선", 다른 하나는 "그 사이 이 정도 나빠지면 주의")을 관측 기간 전체에 대해 실제로 돌려봤더니, 그 기간 내내 한 번도 최종 판정("성공"·"주의")에 도달하지 못했다. 이걸 보고 임계값을 낮추지 않았다 — "판정이 안 나온다"와 "임계값이 잘못됐다"는 다른 문제다. 실제로 확인해보니 이 기간에 주지표 자체가 임계값만큼 움직인 적이 없었을 뿐이었다. 결과가 마음에 안 든다고 기준을 손대면, 그 기준은 더 이상 기준이 아니게 된다.
- **같은 대상을 두 가지 방식으로 셌더니 합계가 안 맞았다.** 전체를 통계적 추정 모델(생존분석 같은)로 센 값과, 같은 구간을 실제 관측된 값 그대로 헤드카운트로 센 값의 합이 서로 달랐다. 처음엔 데이터 누락처럼 보였지만, 원인을 추적해보니 두 계산 방식 자체가 다른 것을 재고 있었다 — 추정 모델은 아직 관측 기간이 덜 찬 대상까지 통계적으로 반영하고, 헤드카운트는 "지금까지 실제로 그 기준을 넘긴 사람"만 센다. 숫자를 억지로 맞추지 않고, 왜 다른지를 기록한 뒤 그대로 넘어갔다.
- **이미 끝난 검증 단계의 통과 기록이 남아있지 않다는 걸 나중에 발견했다.** 판단 자체는 이미 실제 검증 결과(통과/경고/차단 몇 건)를 보고 끝난 상태였는데, 그걸 정식으로 기록해 두지 않았을 뿐이었다. 판단을 다시 하지 않고, 이미 확인했던 실제 결과를 근거로 뒤늦게라도 정식 기록만 남겼다 — "기록이 없다"와 "판단을 안 했다"는 다른 문제다.

### 오늘 배운 일반 원칙

- **판정은 순서가 전부다.** 못 믿을 조건을 먼저 확인하고, 그다음 주지표가 실제로 움직였는지를 보고, 그래야만 부작용(가드레일)을 본다. 앞 단계를 통과했다고 다음 단계를 건너뛰고 곧장 최종 판정으로 가지 않는다.
- **못 믿는 값은 계산해 놓고 숨기는 게 아니라 계산 자체를 하지 않는다.** 감춘 항목에는 "왜 못 믿는지"와 "무엇을 하면 믿을 수 있는지"만 남기고, 실제 값·증감·통계량은 어디에도(요약 화면은 물론, "왜 감췄는지" 설명 화면에도) 남기지 않는다.
- **"정의를 보여주는 것"과 "값을 감추는 것"은 다른 문제다.** 무엇을·어떻게 재는지(계산식, 기준의 근거)는 값이 감춰진 상태에서도 항상 보여줄 수 있다 — 그건 값이 아니라 정의이기 때문이다.
- **화면 상태를 주소(URL)로 공유 가능하게 만들 때는, 그 상태를 바꾸는 조작이 화면 전체가 아니라 일부만 다시 그리는 영역(부분 렌더링) 안에 있으면 주소와 화면이 서로 어긋날 수 있다.** 주소와 동기화해야 하는 조작은 그 부분 렌더링 영역 밖에 둬야 한다.
- **잘못되거나 없는 입력값이 들어와도 앱이 죽으면 안 된다.** 후보 목록에 없는 값, 형식이 이상한 값은 조용히 기본값으로 떨어져야 하고, 에러 화면을 사용자에게 보여줘서는 안 된다.
- **화면에 보이는 순서가 이상해 보이면, 데이터 자체의 순서 문제인지 화면 기능(정렬 등) 때문인지부터 구분해서 확인한다.** 원본 데이터를 직접 찍어보지 않고 화면만 보고 판단하지 않는다.

## Day4 — 오늘 배운 것 (도메인 무관)

### 오늘 내린 결정

- **어느 장을 사람이 쓰기로 했나, 왜**: 배경·해석·제안은 사람이 쓰고 나머지는 자동으로 조립한다. 틀렸을 때 책임을 질 사람이 사라지면 안 되는 자리와, 사실만 그대로 옮기면 되는 자리를 구분했기 때문이다.
- **무슨 표현을 금지했나, 왜**: 기본 금지 표현 6개에 도메인에서 자주 쓰는 인과 단정 표현 2개를 추가했다. 그 과정에서 자동 생성 장에는 이미 걸려 있던 인과 단정 검사가 사람이 쓰는 장에는 안 걸려 있던 것을 발견해서 고쳤다 — 사람이 인과를 더 자주 쓴다는 걸 다시 확인한 사례다.
- **한계 절에 무엇을 적었나**: 검증 경고, 표본이 모자라 판정하지 못한 항목, 항상 넣는 고정 문장 두 개, 그리고 사람이 직접 적어야 하는 "확인하지 못한 것" 세 줄(있는데 안 본 것 / 원래 없는 것 / 찾아봤는데 신호가 없었던 것)로 나눠 적었다. 이 셋은 "경고가 없다"·"안 본 항목이 없다"·"찾아봤는데 없었다"가 서로 다른 사실이라 구분해서 남긴 것이다.

### 오늘 실제로 있었던 판단

- 결과 화면과 문서 출력의 차트·내보내기 기능이 다른 도메인 템플릿에서 넘어온 테이블·컬럼을 그대로 참조하고 있는 버그를 발견했다. 지금 도메인의 실제 테이블과, 표본이 충분한 분해 축 하나로 교체했다.
- 화면에는 이미 걸려 있던 "표본이 모자라면 값을 감추는" 처리가, 같은 값을 보여주는 문서 출력 쪽에는 안 걸려 있던 것을 발견했다. 새로 만들지 않고 같은 판정 로직을 재사용해서 걸었고, 분해 축이 바뀌어도 매번 다시 검사해 "지금은 통과한다고 검사를 생략"하지 않게 만들었다.
- 인과 단정 검사를 걸리면 예외를 던지는 방식으로 구현했었는데, 실제로 걸리는 순간 화면 전체가 죽는다는 설계 오류를 스스로 발견했다(Day1에 정리한 "에러로 죽는 것과 차단으로 멈추는 것은 다르다"는 원칙을 스스로 어긴 것이었다). 화면에 경고만 남기고 멈추지 않는 방식으로 고쳤다.
- 판단 근거를 남기게 되어 있는 확인 단계 두 곳이 실제로 근거를 아무것도 안 적은 채로 통과된 사례가 발생했다. 시스템이 근거 입력을 강제하지 않는다는 것을 그대로 확인했다 — 이건 도구가 대신 채워줄 수 없고 사람이 의식적으로 채워야 하는 부분이라는 교훈으로 남긴다.

## 2026-09-05 (8주차 회고)

### 오늘 실제로 있었던 판단

- 8주차 정리 교안으로 회고하다가, kpis()의 한 지표가 "그레인(사번 1건)"을 지키지 않고 월별·사번별 원본 행을 그대로 평균내고 있던 것을 발견함. 같은 함수 안에서 어떤 지표는 올바르게 사번별로 집계하고 어떤 지표는 안 하고 있어 내부적으로 일관되지 않았음.
- 이 왜곡의 실제 크기(사번별 집계 전후 차이)를 먼저 계산해보고 판단함 — 이 데이터셋에서는 0.42%로 작았지만, "작으니 안 고쳐도 된다"가 아니라 "이미 올바르게 계산해 둔 다른 함수의 값을 재사용"하는 방식으로 그레인 문제와 새 계산을 동시에 없앴음.
- 지표 하나가 "전체 기간 평균"인지 "현재 시점 값"인지 이름만으로는 구분이 안 될 수 있다는 것도 확인함 — 같은 문장 안에 전체기간 평균과 전월 대비 변화를 같이 쓰면 안 됨.
