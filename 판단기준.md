# 판단 기준

## 2026-09-01 (8주차 Day1)

### 오늘 배운 것

**앱이 데이터를 거부할 수 있는가**
거부하지 못하는 앱은 무엇이든 통과시킨다. 컬럼이 하나 빠져도, 날짜가 어긋나도 계산을 해버리고, 그 위에 지은 모든 화면이 틀린다.

**차단과 경고를 가르는 기준**
규칙을 하나 만들 때마다 묻는 것은 하나뿐이다 — "이 규칙이 깨진 채로 계산하면 값이 틀리는가?" 틀린다 → 차단. 해석만 조심하면 된다 → 경고. 이 판정을 붙이는 사람이 이제 나 자신이라는 것이 달라지는 점이다.

**검증은 적게 만든다**
차단을 많이 걸면 꼼꼼해 보이지만, 실무 데이터는 늘 어딘가 깨져 있어서 앱이 자꾸 멈추고, 결국 사람이 차단을 하나씩 끄기 시작하며, 검증 전체가 무력해진다. 검증은 많을수록 좋은 것이 아니라 지켜지는 만큼만 있는 것이 좋다. 차단은 "이대로 계산하면 확실히 틀리는 것"에만 건다. 나머지는 경고다. 깨져도 계산에 영향이 없는 것은 아예 만들지 않는다.

**임계값에는 근거를 적는다**
"몇 개 미만이면 차단인가"에는 정답이 없다. 근거로 쓸 수 있는 것: 직전 기간, 업계 기준, 분석이 성립하는 최소치, 조직의 합의. 어느 것이든 되지만 한 줄로 남겨야 한다. 근거 없는 임계값은 남의 것이다 — 그대로 쓰면 나중에 "왜 이 값이죠?"에 답할 수 없다. 표본 수가 원래 적은 도메인에서 큰 임계값을 요구하면 어떤 세그먼트도 충족하지 못한다. 낮추되 경고 자체를 없애지는 않는다.

**메시지에는 숫자를 넣는다**
"행 수 부족"이 아니라 "12행 (최소 100)"처럼 실제 값과 기준값을 함께 적는다. 그냥 "실패"로는 아무것도 판단할 수 없다. 숫자가 있으면 얼마나 모자란지가 보이고, 그에 따라 대응이 달라진다. 경고라면 왜 정상일 수도 있는지도 함께 적는다 — 사람이 넘길 근거가 된다.

**구조적 결측을 만나면**
빈 칸이 다 오류는 아니다. 이번 기간에 그 사건이 일어나지 않아서, 그 경로를 거치지 않아서, 아직 그 단계에 도달할 시간이 없어서 비어 있는 것은 정상이다. 필수 입력인데 비어 있는 값만 오류다. 구조적 결측을 채우면 데이터를 위조하는 것이다 — 없는 것이 사실이기 때문이다. 평균이나 0으로 채우면 "그 조건에 해당하지 않는 대상"이 "값이 0인 대상"으로 둔갑한다. 이런 결측은 경고로 띄우되 차단하지 않는다. 다만 조인할 때 조용히 사라지므로 그 대상이 어디로 갔는지 알고 있어야 한다.

**검증은 만든 것으로 끝나지 않는다**
실제로 막히는지 눈으로 봐야 한다. 경고만 띄우고 그냥 진행되는 검증은 없는 것과 같다. 확인할 것 셋 — 차단 표시가 뜬다, 차단 개수가 세어진다, 통과 버튼이 잠긴다. 마지막이 가장 중요하다. 버튼이 안 잠기면 그 검증은 장식이다. 앱이 에러로 죽는 것과 차단으로 멈추는 것은 다르다 — 죽으면 사람이 판단할 화면 자체가 없다.

**판단 근거를 남긴다**
"확인함" 같은 말은 근거가 아니다. 무엇을 보고 왜 괜찮다고 판단했는지 적는다. 경고가 없다면 "경고 없음, 검증 n건 전부 통과"라고 명시적으로 적는다. 아직 다음 단계로 갈 시간이 없는 대상 때문에 비율이 낮게 나올 수 있다 — 이것을 "성과가 나쁘다"로 읽으면 틀린다. 관측 기간 문제와 실제 문제를 구분해야 한다.

**차단/경고의 공통 패턴**
차단 = 키가 없거나 · 중복이거나 · 순서가 역행하거나 · 값이 불가능하다.
경고 = 비어 있거나 · 적거나 · 치우쳐 있다.

**"설정 하나만 고치면 된다"는 절반만 맞다**
이름·기간·단계를 내 것으로 바꾸고 데이터를 연결한 뒤 앱을 켜면 깨진다. 컬럼 이름이 계산 코드 안에도 박혀 있기 때문이다. 한 번에 되면 오히려 뭔가 안 바꾼 것이다.

### 오늘 내가 실제로 내린 결정

- **행 수 규칙을 차단으로 정했다** — 표본이 최소 표본 미만이면 어떤 코호트 분석도 성립하지 않기 때문이다.
- **필수 컬럼 규칙을 차단으로 정했다** — 그 컬럼들이 없으면 잔류율 퍼널과 유지/이탈 판정 자체를 계산할 수 없기 때문이다.
- **날짜 범위 규칙을 차단으로 정했다** — 기간 밖 데이터가 섞이면 집계값이 그대로 왜곡되기 때문이다.
- 오늘 만든 세 규칙은 각각 "이게 깨지면 계산이 틀리는가"를 물었을 때 셋 다 "틀린다" 쪽으로 나와 전부 차단으로 걸었다. 별도의 경고 규칙은 아직 추가하지 않았다.
- **최소 표본을 30으로 잡았다** — 이미 다른 분석에서 쓰고 있던 "코호트 분석이 성립하는 최소 표본" 기준을 그대로 가져왔다. 기본으로 주어진 값은 표본 수 자체가 작은 도메인에는 맞지 않아 낮췄다.
